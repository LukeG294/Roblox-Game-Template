-- Easy-to-change settings for the simple MVP round loop.
-- Timers are in seconds.
local RoundConfig = {
    IntermissionDuration = 10,
    RoundDuration = 60,

    -- Text shown to players during each phase.
    IntermissionStatus = "Intermission",
    RoundStatus = "Round in progress",
}

return RoundConfig
